services:
  postgres:
    image: postgres:18
    environment:
      POSTGRES_DB: salesmanagement
      POSTGRES_USER: salesmanagement
      POSTGRES_PASSWORD: salesmanagement
    ports:
      - "5433:5432"
    volumes:
      - postgres-data:/var/lib/postgresql

volumes:
  postgres-data:
